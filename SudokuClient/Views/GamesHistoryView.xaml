<UserControl x:Class="SudokuClient.Views.GamesHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SudokuClient.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="450" Background="white">
    <Grid>
        <ListBox Margin="20" ItemsSource="{Binding PreviousGames}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Path=Level}"/>
                        <TextBlock Text=" "/>
                        <TextBlock Text="{Binding Path=Duration}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
